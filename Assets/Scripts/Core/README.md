# Core 模块说明

本模块负责垃圾分类游戏的运行时流程控制：

- 启动并绑定当前场景
- 订阅分类结果事件
- 管理倒计时、分数、进度与输入启停
- 在成功或失败时触发结算
- 读取可选的场景任务配置

`WasteGameBootstrap` 作为运行时入口自动创建，避免手动把管理脚本挂到每个场景。
`WasteGameFlowController` 只处理游戏流程，不直接生成分类结果，也不在这里实现分类规则。
`WasteGameSceneConfig` 可选挂到场景任意对象上，用于配置目标数量和总时长。
